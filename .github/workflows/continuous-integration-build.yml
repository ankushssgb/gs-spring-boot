name: Continuous Integration Pipeline
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ci-build-and-test:
    name: Build and Validate Spring Boot Application
    uses: spring-guides/getting-started-macros/.github/workflows/build_initial_complete_maven_gradle.yml@main
